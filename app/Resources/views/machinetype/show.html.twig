{% extends 'base.html.twig' %}

{% block body %}
    <h1>MachineType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ machineType.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ machineType.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ machineType.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('machinetype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('machinetype_edit', { 'id': machineType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
